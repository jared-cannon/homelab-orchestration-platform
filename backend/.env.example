# Server Configuration
PORT=8080
ENV=development

# Database Configuration
DB_PATH=./homelab.db

# Authentication Configuration
# Set to 'true' to require authentication for all API endpoints (recommended for production)
# Set to 'false' or leave empty to disable authentication (development only)
REQUIRE_AUTH=true

# JWT Secret Key (REQUIRED for authentication)
# Generate a secure random string for production:
# Example: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# CORS Configuration
# Comma-separated list of allowed origins
# Example: http://localhost:5173,http://192.168.1.100:5173
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
